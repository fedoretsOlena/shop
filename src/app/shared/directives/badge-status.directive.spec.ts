import { BadgeStatusDirective } from './badge-status.directive';

describe('BadgeStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeStatusDirective();
    expect(directive).toBeTruthy();
  });
});
